# Currency Dashboard

Projeto que consome uma API pública para buscar as cotações de várias moedas de diferentes países.

Utilizamos `requests` para fazer as chamadas à API, `pandas` para organizar os dados recebidos, e `matplotlib` para gerar gráficos comparando as cotações.